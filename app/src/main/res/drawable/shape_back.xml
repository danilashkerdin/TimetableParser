<?xml version="1.0" encoding="utf-8"?>

<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle"> <!-- View background color -->
    <solid android:color="@android:color/white"></solid> <!-- View border color and width -->
    <stroke
        android:width="@dimen/border_with"
        android:color="@color/colorPrimary" />
    <corners android:radius="@dimen/cardview_radius"></corners>
</shape>
